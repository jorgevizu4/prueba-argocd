apiVersion: v1
kind: Service
metadata:
  name: ejemplo-service
  namespace: argocd
spec:
  selector:
    app: ejemplo
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP